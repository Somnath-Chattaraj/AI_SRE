First integrate patcher-rag folder with sre-anomaly. Take the incident report from prisma db instead of the json file. Also change openrouter to cerebrus. there is a cerebrus ai sdk package. Also the file read and write is very immature use some good grep,read wite tools that opencode or claude code uses to make perfect changes and exact the position you want. When an incident occurs it should automatically call the patcher-rag action to fix the file in a different branch and make a pr. Also use scalable architecture and best practices and try to use opensource libraries as much as possible to keep the code clean.